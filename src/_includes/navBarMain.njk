<!-- navBarMain starts -->
<nav class="main-nav no-print">
    <div class="nav-container">
        <a href="/" class="link">{{ site.author.name }}</a>
        <button class="nav-toggle" aria-label="toggle navigation">
            {# <img src="{{ site.baseURL }}/assets/icons/hamburguer.svg" class="icon-open" alt="Abrir menú"> #}
            <div  class="icon-open" alt="Abrir menú">
                {% svg 'hamburguer.svg' %}
            </div>
            <div class="icon-close" alt="Cerrar menú">
                {% svg 'squarex.svg' %}
            </div>
            {# <img src="{{ site.baseURL }}/assets/icons/squarex.svg" class="icon-close" alt="Cerrar menú"> #}
        </button>
        <ul class="nav-links">
            {% for section in site.sections %}
                <li class="link">
                    <a href="{{ section.url }}" class="links">{{ section.name }}</a>
                </li>
            {% endfor %}  
            <li class="link link_icon_2">
            <li id="theme-toggle" aria-label="Toggle dark mode" class="">
                
                <span class="dark-mode-text"><div>Light</div> {% svg 'sun.svg' %}</span>                     
                <span class="light-mode-text" style="display: none;">Dark{% svg 'moon.svg' %}</span>
                    
               
            </li>
        </li>
        </ul>
    </div>
</nav> 
<!-- navBarMain ends --> 